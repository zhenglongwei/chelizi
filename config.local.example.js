/**
 * 参考示例（小程序不会加载本文件）。
 * 推荐：在 web/.env 设置 ZHEJIAN_MINIPROGRAM=local 与 ZHEJIAN_LOCAL_API_BASE，然后执行 npm run sync:config。
 * 若坚持手改，可将下方导出复制到 config.local.js（勿提交本地地址到远程）。
 */
module.exports = {
  BASE_URL: 'http://127.0.0.1:3000',
};
