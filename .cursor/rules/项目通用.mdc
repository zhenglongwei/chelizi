---
description: 辙见项目通用约束（语言、需求沟通、文档基准）
alwaysApply: true
---

# 辙见项目通用规范

## 语言与基准

- 所有回复使用**中文简体**
- 产品名：**辙见**
- 需求基准：`辙见需求文档.md`、`辙见-全流程需求梳理.md`
- UI 基准：`设计规范.md`

## 线上目录约定（强制）

- 默认按用户当前服务器结构执行命令：`/var/www/simplewin` 下**直接包含** `api-server/`、`scripts/`、`database/`、`.env`（无 `web/` 中间层）。
- 未经用户确认，禁止假设线上路径为 `/var/www/simplewin/web/...`。
- 给出服务器命令前，优先使用上述扁平目录；如需兼容两种目录结构，应同时给出两套命令并标注推荐路径。

## 需求沟通（强制）

- 收到需求后**先提问**：明确真实意图、边界、特殊情况
- 确认无歧义后再写代码，**禁止一上来就写代码**
- 涉及新模块/新能力/外部渠道对接时，**先做市场前景与落地可行性评估**，再进入开发。
- 评估至少包含：目标用户与频次、可替代方案、外部依赖可得性（API/渠道/授权）、成本与周期、主要风险与回退方案。
- 若关键前提不成立（如无公开可用渠道、无授权、无稳定数据源），应先向用户明确“暂不具备落地条件”，由用户确认后再决定是否继续开发。

## 最小可用闭环

- 实现到能开发/测试/查询/查看/提交，具备基础错误提示

## 文档同步

- 功能/字段/接口变化 → 按 `规范/文档同步.md` 更新相关文档
- 评价/激励/用户等级/店铺/排序/竞价变更 → 同步修改 `docs/体系/` 对应文档，**禁止新增**
