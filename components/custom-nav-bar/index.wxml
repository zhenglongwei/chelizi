<!-- 自定义导航栏：标题垂直居中，适配刘海屏等；通过 navigate 进入的子页可加 showBack；支持左上角插槽 leftSlot（避开微信右上角胶囊） -->
<view class="custom-nav-bar" style="height: {{navBarHeight}}px; background-color: {{backgroundColor}};">
  <view class="nav-bar-status" style="height: {{statusBarHeight}}px;"></view>
  <view class="nav-bar-content" style="height: {{navContentHeight}}px;">
    <view class="nav-bar-left" wx:if="{{showBack}}">
      <view class="nav-bar-back" bindtap="onBack">
        <image class="nav-bar-back-icon" src="/images/icon/back.png" mode="aspectFit"/>
        <text class="nav-bar-back-text">返回</text>
      </view>
    </view>
    <view class="nav-bar-left nav-bar-left-slot" wx:elif="{{showRight}}">
      <slot name="right"></slot>
    </view>
    <view class="nav-bar-title" style="color: {{textColor}};">{{title}}</view>
    <view class="nav-bar-placeholder" wx:if="{{showBack || showRight}}"></view>
  </view>
</view>
