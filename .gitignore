# 依赖
node_modules/

# 构建产物
web/dist/
web/dist-ssr/
*.local

# 小程序本地 API 地址（勿提交）
config.local.js

# 环境变量（含密钥，勿提交）
.env
.env.local
.env.production
.env.development
web/scripts/cron.env

# 微信支付商户证书 / 私钥（仅放服务器或本机私密目录，勿提交仓库）
# 建议整目录存放，例如项目外 /var/www/.../certs/；若必须放在仓库旁，可用 ./certs/ 并忽略：
certs/
web/certs/
secrets/
apiclient_key.pem
apiclient_cert.pem
apiclient_cert.p12

# 日志
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 上传目录（用户上传的图片等，服务器单独维护）
web/api-server/uploads/

# 编辑器与系统
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 微信开发者工具
project.private.config.json
