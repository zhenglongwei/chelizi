/**
 * 竞价邀请列表 - M04
 * 参考电商订单卡片：主标题醒目、副信息紧凑、辅助信息弱化
 * 以《设计规范.md》、design-system 为准
 */
.merchant-bidding-list {
  min-height: 100vh;
  background: var(--bg-page);
  padding: var(--space-lg) var(--page-padding-x);
}

/* ========== Tab ========== */
.merchant-tabs {
  display: flex;
  gap: var(--space-xl);
  margin-bottom: var(--space-xl);
  padding: 0 var(--space-xs);
}

.merchant-tab {
  padding: var(--space-sm) 0;
  font-size: var(--fs-body);
  color: var(--text-secondary);
  position: relative;
}

.merchant-tab.active {
  color: var(--primary);
  font-weight: 500;
}

.merchant-tab.active::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 4rpx;
  background: var(--primary);
  border-radius: 2rpx;
}

/* ========== 卡片（电商订单式：主次分明） ========== */
.merchant-list-scroll {
  height: calc(100vh - 200rpx);
}

.merchant-bidding-card {
  padding: var(--space-xl);
  margin-bottom: var(--space-lg);
  transition: transform 0.2s ease;
}

.merchant-bidding-card:active {
  transform: scale(0.995);
}

/* 主标题行：车辆信息 + 状态标签 */
.card-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: var(--space-md);
  margin-bottom: var(--space-sm);
}

.card-title {
  flex: 1;
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  color: var(--text-primary);
}

.card-tags {
  flex-shrink: 0;
  display: flex;
  gap: var(--space-xs);
}

.card-tag {
  padding: var(--space-xs) var(--space-sm);
  border-radius: var(--radius-sm);
  font-size: var(--fs-sm);
  font-weight: 500;
}

.card-tag.quoted {
  color: var(--success);
  background: rgba(34, 197, 94, 0.1);
}

.card-tag.won {
  color: var(--success);
  background: rgba(34, 197, 94, 0.1);
}

.card-tag.invalid {
  color: var(--text-secondary);
  background: var(--bg-placeholder);
}

/* 副信息行：复杂度 · 时效 · 距离（紧凑排列） */
.card-detail {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: var(--space-xs);
  margin-bottom: var(--space-xs);
}

.card-sep {
  color: var(--text-hint);
}

.card-countdown {
  color: var(--warning);
  font-weight: 500;
}

/* 辅助信息：创建时间（弱化、右对齐） */
.card-foot {
  text-align: right;
}

/* ========== 空态 / 加载 ========== */
.merchant-empty,
.merchant-loading {
  padding: var(--space-2xl);
  text-align: center;
}
